# NWXT Gen Mobile

This directory contains the Expo React Native mobile app for the NWXT roadmap.

## Available scripts

- `npm install` — install dependencies
- `npm run start` — start Expo development server
- `npm run android` — open app on Android device or emulator
- `npm run ios` — open app on iOS simulator
- `npm run web` — run the app in the browser
