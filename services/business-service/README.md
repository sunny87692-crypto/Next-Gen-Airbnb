# Business Service

Java Spring Boot microservice placeholder for the NWXT roadmap application.

## Usage

1. Build with Maven: `mvn clean package`
2. Run: `mvn spring-boot:run`
3. Health endpoint: `GET http://localhost:8080/health`
