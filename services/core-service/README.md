# Core Service

A starter Go + Gin microservice for the NWXT roadmap application.

## Usage

1. Install Go modules: `go mod tidy`
2. Run the service: `go run main.go`
3. Health endpoint: `GET http://localhost:5001/health`
